# One thing to note that strings are immutable,
# means they can't be changed by using any kind
# of functions
# They are literals(constants) values which are not meant to be
# changed.

name = "Divyanshu"
slice = name[1:8]
print(slice)

word = "amazing"
word_slice = word[1:6:1]
print(word_slice)